[package]
name = "steward-eventlog"
description = "The names, GUIDs and manifest of steward's per-user Event Log channels"
version.workspace = true
edition.workspace = true
license.workspace = true
publish.workspace = true
